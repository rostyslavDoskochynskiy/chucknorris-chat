import React from 'react';
import LinearProgress from '@material-ui/core/LinearProgress';

/**
 * Description:
 *
 * Component for linear progress
 */
export const LinearProgressBase = props => <LinearProgress {...props} />;
